name: CI/CD

on: 
    push: 
        branches: 
            -"master"

jobs:
    hello-world:
        runs-on: 
            - ubuntu-latest
        steps:
            - name: "Print hello world msg"    
              run: |
                echo hello world
                echo print msg2   