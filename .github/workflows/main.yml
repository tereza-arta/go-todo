name: CI/CD

on: 
    push: 
        branches: 
            - "master"

jobs:
    build:
        runs-on:
            - "ubuntu-latest"
        steps:
            - name: "checkout"
              run: echo Hello world